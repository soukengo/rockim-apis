syntax = "proto3";

package rockim.shared.reasons;

option go_package = "rockimserver/apis/rockim/shared/reasons";
option java_multiple_files = true;
option java_package = "cn.rockim.shared.reasons";

message Group {
  enum ErrorReason {
    // 未知错误
    UN_SPECIFIED = 0;
    // 群组不存在
    GROUP_NOT_FOUND = 1;
    // 群组重复
    GROUP_DUPLICATED = 2;
    // 群组成员不存在
    GROUP_MEMBER_NOT_FOUND = 3;
  }

}