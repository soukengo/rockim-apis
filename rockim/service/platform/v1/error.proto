syntax = "proto3";

package logic.user.v1;

option go_package = "rockimserver/apis/rockim/service/logic/platform/v1;v1";

enum ErrorReason {
  // 未知错误
  UN_SPECIFIED = 0;
  // 平台用户不存在
  PLAT_USER_NOT_FOUND = 1;
  // 账号重复
  PLAT_USER_ACCOUNT_DUPLICATE = 2;
  // 商户不存在
  TENANT_NOT_FOUND = 3;
  // 商户账号重复
  TENANT_ACCOUNT_DUPLICATE = 4;
  // 应用不存在
  APP_NOT_FOUND = 5;
}
