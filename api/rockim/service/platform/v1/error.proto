syntax = "proto3";

package logic.user.v1;

option go_package = "rockim/api/rockim/logic/platform/v1;v1";

enum ErrorReason {
  // 未知错误
  UN_SPECIFIED = 0;
  // 平台用户不存在
  PLAT_USER_NOT_FOUND = 1;
  // 账号重复
  ACCOUNT_DUPLICATE = 2;
  // 租户不存在
  TENANT_NOT_FOUND = 3;
  // 应用不存在
  APP_NOT_FOUND = 4;
}
