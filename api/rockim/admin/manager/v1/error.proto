syntax = "proto3";

package rockim.admin.manager.v1;

option go_package = "rockim/api/rockim/service/user/v1;v1";

enum ErrorReason {
  // 未知错误
  UN_SPECIFIED = 0;
  // 账号不存在
  ACCOUNT_NOT_FOUND = 1;
  // 密码不正确
  PASSWORD_INCORRECT = 2;
  // 授权无效
  AUTHORIZATION_INVALID = 3;
}
