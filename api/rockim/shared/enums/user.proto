syntax = "proto3";

package rockim.shared.enums;

option go_package = "rockim/api/rockim/shared/enums;enums";

// UserStatus 用户状态
enum UserStatus {
  // 无效
  USER_STATUS_INVALID = 0;
  // 正常
  USER_STATUS_NORMAL = 1;
}