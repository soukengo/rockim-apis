syntax = "proto3";

package rockim.shared.types;

option go_package = "rockim/api/rockim/shared/types";

// Paginating 分页参数
message Paginating {
  int32 page_no = 1;
  int32 page_size = 2;
}

// Paginated 分页结果
message Paginated {
  int64 total = 1;
}